# ============================================
# REQUIRED CONFIGURATION
# ============================================

# Home Assistant URL (include http:// or https://)
# Example: http://192.168.1.100:8123 or http://homeassistant.local:8123
HASS_URL=http://homeassistant.local:8123

# Home Assistant Long-Lived Access Token
# Get this from: Profile > Long-Lived Access Tokens > Create Token
HASS_TOKEN=your_long_lived_access_token_here

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Server Environment (development or production)
NODE_ENV=production

# Log Level (error, warn, info, debug)
LOG_LEVEL=info

# Server Port (only used for HTTP mode, not stdio)
PORT=7123

# ============================================
# ADVANCED CONFIGURATION (Optional)
# ============================================

# Enable stdio transport (required for MCP clients)
USE_STDIO_TRANSPORT=true

# WebSocket URL (auto-constructed from HASS_URL if not set)
# HASS_SOCKET_URL=ws://homeassistant.local:8123/api/websocket

# ============================================
# Advanced Features
# ============================================

# Enable advanced operations
ENABLE_SHELL_COMMANDS=false
ENABLE_FILE_OPERATIONS=false
ENABLE_YAML_EDITOR=false
ENABLE_SYSTEM_RESTART=false
ALLOW_CONFIG_MODIFICATIONS=false
ALLOW_SUPERVISOR_ACCESS=false
ALLOW_NETWORK_OPERATIONS=false
ALLOW_ADDON_INSTALLATION=false
ALLOW_BACKUP_OPERATIONS=false

# ============================================
# SPEECH FEATURES (Experimental - Disabled by Default)
# ============================================

# Enable speech-to-text features (requires additional setup)
ENABLE_SPEECH_FEATURES=false
ENABLE_WAKE_WORD=false
ENABLE_SPEECH_TO_TEXT=false

# ============================================
# NOTES
# ============================================

# This is a simplified configuration file for MCP stdio usage.
# For full configuration options, see the original .env.example.backup

# Required for MCP clients (VS Code, Cursor, Claude Desktop):
# - HASS_URL: Your Home Assistant instance URL
# - HASS_TOKEN: Your long-lived access token

# Everything else has sensible defaults and can be left as-is.
